# T2.5 — Redis Testcontainers Pilot Plan

| Field | Value |
|-------|-------|
| ID | T2.5 |
| Type | Implementation Plan |
| Status | Draft pilot plan |
| Filename | `T2.5-redis-testcontainers-pilot-plan.md` |
| Depends on | T2.4 |
| Pilot target | Redis |
| Follow-up candidate | Kafka + Schema Registry |

---

## 1. Purpose

This document defines the Redis Testcontainers pilot scope, implementation approach, risks and measurement plan. Redis is selected as a low-complexity first pilot to validate Testcontainers wiring and workflow.

This pilot is not intended to solve the main Kafka topic/message/offset isolation path. T2.4 found that Kafka + Schema Registry are the central integration-test input/assertion path and remain the higher-value follow-up candidate.

---

## 2. Decision Summary

Decision:

`Use Redis as the first Testcontainers pilot.`

Rationale:

- Lower implementation complexity than Kafka + Schema Registry.
- Suitable for one-engineer pilot.
- Validates Testcontainers dependency wiring pattern.
- Allows limited-scope learning without redesigning Kafka, Schema Registry, aggregators or command-adaptor orchestration.

Non-goals:

- Do not claim to solve Kafka topic/message/offset isolation.
- Do not claim CI time saving before measurement.
- Do not replace the full docker-compose integration-test stack.
- Do not edit centrally controlled `pre-integration-test/app.py` locally unless the upstream RepoSync source is changed.
- Do not claim flaky tests are fixed.

### Implementation Decision

- Phase 1 will be an opt-in local Redis Testcontainers smoke/wiring test.
- Phase 1 will not replace docker-compose Redis.
- Phase 1 will not modify `pre-integration-test/app.py`.
- Phase 1 will not change existing `local-int-cmd` or `local-int-snapshot` behaviour.
- Phase 1 will not be wired into CI by default.
- CI integration and Redis override are later decisions after the smoke pilot succeeds.

---

## 3. Current Redis Usage

The reviewed `pre-integration-test/app.py` is centrally controlled and contains the warning `DO NOT EDIT LOCALLY`. It also states that local changes in the command-adaptor repository will be overwritten by RepoSync. The Redis pilot should therefore avoid directly editing this file unless the change is made in the central source/repo-sync owner path.

This is confirmed by the reviewed `reposync.py`, which copies `pre-integration-test/app.py` from the central RepoSync source into each adaptor repository under the integration-test docker-compose resources. The same RepoSync flow also applies the central `docker-compose.yml` template into the adaptor integration-test docker-compose directory.

Therefore, local changes to `pre-integration-test/app.py` or generated docker-compose resources should not be part of the Phase 1 Redis pilot unless the change is made through the upstream RepoSync-owned source path.

Observed Redis behaviour in `pre-integration-test/app.py`:

- `WAIT_CHECK` determines the startup stage.
- Startup stage `1` maps to `redis_kafka`.
- When `WAIT_CHECK == "redis_kafka"`, the script creates a Redis client using `redis.Redis("redis")`.
- The script waits until `rs.ping()` succeeds.
- The same stage then waits for Schema Registry, waits for Kafdrop, sorts Kafka topics and creates them via `create_kafka_topics("kafka:29092", topic_names)`.

| Area | Current behaviour | Pilot implication |
|------|-------------------|-------------------|
| `pre-integration-test/app.py` | Uses `redis.Redis("redis")` during `WAIT_CHECK=redis_kafka` and waits for `ping()` | Redis hostname is hard-coded in startup utility |
| `redis_kafka` stage | Waits for Redis, Schema Registry, Kafdrop, then creates Kafka topics | Redis pilot cannot blindly replace the whole stage |
| Docker Compose | Provides Redis service and network hostname `redis` | Testcontainers pilot must decide whether to replace, override, or run alongside compose Redis |
| Java test path | No direct Redis assertion path observed in `SnsSteps.java` | Redis pilot is infrastructure/support scope, not central assertion scope |

The reviewed `README.md` describes a Maven multi-module project with `cmd-adaptor-sns-common`, `cmd-adaptor-sns` and `cmd-adaptor-sns-integration-tests`. The integration-test module is a full containerised docker-compose build with BDD Cucumber feature-file driven E2E tests.

Existing integration-test workflow:

- `local-int-cmd` starts containers, but not the Aker aggregators.
- `local-int-snapshot` starts containers, including the Aker aggregators.
- `mvn -Plocal-int-cmd clean install` runs the command-adaptor local integration path.
- `mvn -Plocal-int-snapshot clean install` runs the snapshot local integration path.
- `mvn integration-test -Plocal-int-snapshot` leaves containers up and reduces the build-test cycle.
- `mvn clean integration-test -Plocal-int-snapshot` restarts containers and clears Kafka topics.

The Redis pilot must fit this existing Maven/profile/docker-compose developer workflow and should avoid disrupting the documented local integration-test path.

---

## 4. Pilot Scope Options

### Option A — Minimal learning pilot

Description:

Add a small Testcontainers-based Redis smoke/integration test that validates Redis container startup and configuration wiring without changing the full existing E2E flow.

Pros:

- Lowest risk.
- Does not disturb current docker-compose E2E tests.
- Good first proof of Testcontainers in this repo.

Cons:

- Does not replace the compose Redis service.
- Limited value for CI timing.
- Does not prove full E2E compatibility.

Conclusion:

Selected as the Phase 1 implementation route.

### Option B — Local profile Redis override pilot

Description:

Introduce a dedicated Maven/profile or test configuration path that starts Redis via Testcontainers and points the relevant runtime config to the Testcontainers Redis host/port for selected local testing.

Pros:

- More meaningful than a smoke test.
- Still smaller than Kafka + Schema Registry.
- Can validate wiring pattern.

Cons:

- Existing `pre-integration-test/app.py` hard-codes `redis.Redis("redis")`.
- May require configuration or compose override design.
- Must avoid editing central RepoSync-controlled script locally.

Conclusion:

Recommended only after Option A succeeds and is reviewed.

### Option C — Replace compose Redis in full E2E flow

Description:

Remove or bypass compose Redis and use Testcontainers Redis in the current full integration-test flow.

Pros:

- Highest Redis-specific value.

Cons:

- Riskier.
- Docker network/hostname compatibility issue likely.
- `pre-integration-test/app.py` expects hostname `redis`.
- Full E2E flow still depends on Kafka, Schema Registry, aggregators and command adaptor.
- Could require broader orchestration redesign.

Conclusion:

Not recommended as the first implementation step.

---

## 5. Recommended Pilot Route

Recommended route:

`Option A first. Option B only after Option A succeeds and is reviewed.`

Given this pilot is likely to be progressed by one engineer and the current startup utility is centrally controlled, the safest route is:

1. Add a minimal Redis Testcontainers smoke/wiring test.
2. Use the result to validate dependencies, Maven setup and local developer workflow.
3. Then design a local/profile-based Redis override only if the smoke pilot is accepted.
4. Keep full E2E docker-compose flow unchanged initially.

Option A success criteria:

- Redis Testcontainers smoke test passes locally.
- Existing docker-compose E2E commands remain unchanged.
- No RepoSync-managed file is edited.
- Pilot test can be run independently of the full E2E stack.
- Added dependencies do not break existing build.
- Execution time is recorded.
- Limitations are documented.

This route validates Testcontainers with a small implementation surface while avoiding claims that Redis is the main integration-test assertion path.

---

## 6. Phase 1 Implementation Scope

### In scope

- Confirm or add Testcontainers dependencies in the integration-test module.
- Create `MinimalRedisTest.java`.
- Start Redis with Testcontainers.
- Connect using a Redis Java client already available or a minimal test dependency if needed.
- Perform a simple set/get or ping validation.
- Run the test under an added or confirmed opt-in Maven profile.
- Keep test separate from Cucumber E2E features.
- Keep existing docker-compose flow unchanged.

### Out of scope

- Replacing docker-compose Redis.
- Replacing `WAIT_CHECK=redis_kafka`.
- Editing `pre-integration-test/app.py`.
- Changing RepoSync-managed source.
- Wiring the pilot into CI by default.
- Reworking Kafka, Schema Registry, aggregators or command-adaptor orchestration.
- Measuring or claiming CI savings.
- Claiming flaky-test reduction.

---

## 7. Test Framework and Maven Execution

Current repo verification has not yet confirmed a local Testcontainers Maven profile, `MinimalRedisTest.java`, or Testcontainers Maven dependencies. These must be added or confirmed in `cmd-adaptor-sns-integration-tests/pom.xml` before implementation.

The root POM module list shows the integration-test module path as `cmd-adaptor-sns-integration-tests`, so any `-pl` command should initially be validated against that module path rather than assuming an artifactId selector.

Example command shape:

```bash
mvn -pl cmd-adaptor-sns-integration-tests -P<redis-testcontainers-profile> test
```

The profile name and plugin configuration are not confirmed yet.

---

## 8. Redis Image Decision

For Phase 1, use the same Redis image family as the current docker-compose baseline unless the team explicitly chooses otherwise.

Recommended image:

`redis:5.0.6`

Rationale:

- Keeps the pilot behaviour close to current baseline.
- Avoids mixing Testcontainers introduction with a Redis version upgrade.
- Redis version upgrade/support posture should remain a separate decision.

---

## 9. Proposed Technical Approach

### Phase 1 — Dependency and smoke test

- Confirm whether Testcontainers dependencies are present in `cmd-adaptor-sns-integration-tests/pom.xml`. If not present, add them in test scope or through existing dependency management.
- Create `MinimalRedisTest.java` and configure an opt-in Maven profile or include pattern to run it without triggering the existing full E2E path.
- Use `GenericContainer` if no dedicated Redis module is already used.
- Use the current Redis image baseline, `redis:5.0.6`, unless the team explicitly chooses another image.
- Choose JUnit 4/Failsafe or JUnit 5/Jupiter based on the existing `cmd-adaptor-sns-integration-tests` POM setup.
- Validate local execution.

The smoke test should:

- Start a Redis container through Testcontainers.
- Connect to the mapped Redis endpoint.
- Run a minimal ping or set/get validation.
- Stop the container through the normal Testcontainers lifecycle.
- Avoid Cucumber, docker-compose, Kafka, Schema Registry, aggregators and command-adaptor startup.

### Phase 2 — Maven profile / opt-in execution

- Define or confirm an opt-in local Testcontainers profile or test include pattern.
- Ensure default E2E flow is unchanged.
- Ensure README commands remain valid.
- Keep Phase 1 local-first and opt-in.
- Do not wire the pilot into CI by default.

### Phase 3 — Review results

- Capture whether the test passes reliably.
- Capture execution time.
- Capture any dependency or Docker environment issues.
- Confirm existing docker-compose E2E commands still behave as before.
- Decide whether to proceed to Option B.

### Phase 4 — Optional Redis override design

Only after Phase 1-3 succeed and are reviewed, investigate whether the runtime Redis endpoint can be externalised enough to point at Testcontainers Redis.

This phase may evaluate:

- A profile-based Redis endpoint override.
- A Docker network alias.
- A central RepoSync change if the startup utility must be changed.
- Whether the command-adaptor repo can avoid local-only edits to `pre-integration-test/app.py`.

Do not attempt to replace the whole `redis_kafka` startup stage in the first pilot.

---

## 10. Risks and Constraints

| Risk | Why it matters | Mitigation |
|------|----------------|------------|
| `app.py` is centrally controlled | Local edits may be overwritten by RepoSync | Avoid local edits or change the central source |
| RepoSync-managed file overwrite | `reposync.py` copies `pre-integration-test/app.py` and applies docker-compose resources into adaptor repos | Do not rely on local edits to these files for Phase 1 |
| Central ownership path required | Changes to generated integration-test resources may belong in RepoSync source, not individual adaptor repos | Treat any such change as an upstream RepoSync decision |
| Redis hostname is hard-coded as `redis` | Testcontainers exposes dynamic host/port by default | Start with smoke test; later evaluate network alias or config override |
| `redis_kafka` stage combines Redis + Kafka readiness | Redis is not isolated in the startup utility | Do not replace the whole stage in first pilot |
| Full E2E still depends on Kafka/Schema Registry | Redis pilot will not validate central assertion path | Explicitly keep Kafka + Schema Registry as follow-up |
| CI timing improvement may be small | Redis is not the dominant observed test cost | Do not claim savings until measured |
| Existing developer workflow relies on docker-compose profiles | Breaking local workflow would reduce adoption | Keep existing profiles unchanged in first step |
| Pilot becomes too broad | It could turn into Kafka/compose redesign | Keep Phase 1 to smoke/wiring only |
| CI adoption too early | Could disrupt team builds before the approach is proven | Keep Phase 1 local/opt-in |
| Redis version upgrade mixed with Testcontainers pilot | Makes results harder to interpret | Use current Redis baseline image first |
| Test framework churn | Adding a new test framework increases scope | Align with existing module test framework |

---

## 11. Measurement Plan

Before implementation:

- Current T2.4 outer CI Integration Tests baseline: `09:43` average.
- Current T2.4 full CI average: `13:35`.
- Existing local E2E commands from README remain baseline commands.
- Record current local command duration if available.
- Record developer workflow impact qualitatively.

Phase 1 measurement:

- Redis smoke test execution time.
- Whether it runs without docker-compose.
- Whether existing E2E workflow remains unchanged.
- Whether it requires CI changes; expected answer should be no for Phase 1.
- Any Docker/Testcontainers environment issues.

After Phase 2/3:

- Record whether the pilot can run independently.
- Record whether existing docker-compose E2E path remains unchanged.
- Record any CI impact if it is later wired into CI.

Do not compare Phase 1 smoke-test time directly against full E2E time as a saving.

Phase 1 validates feasibility, not performance saving.

Do not claim:

- CI saving
- flaky-test reduction
- Kafka isolation improvement

until measured.

---

## 12. Acceptance Criteria

- [ ] Testcontainers dependency approach is confirmed or added for `cmd-adaptor-sns-integration-tests`.
- [ ] Redis image choice is confirmed; default recommendation is current baseline `redis:5.0.6`.
- [ ] `MinimalRedisTest.java` is created.
- [ ] Test framework choice is confirmed against `cmd-adaptor-sns-integration-tests/pom.xml`.
- [ ] Opt-in Maven profile or test include pattern is defined.
- [ ] Existing docker-compose E2E flow remains unchanged.
- [ ] Existing README commands remain valid.
- [ ] No local-only edit is made to centrally controlled `pre-integration-test/app.py`.
- [ ] Pilot can run without starting the full E2E docker-compose stack.
- [ ] Phase 1 measurement approach is defined.
- [ ] Pilot limitation is documented: Redis does not validate the main Kafka assertion path.
- [ ] Kafka + Schema Registry follow-up remains documented.

---

## 13. Not Yet Claimed

- No Redis Testcontainers implementation is complete yet.
- No CI saving is claimed.
- No flaky-test improvement is claimed.
- No Kafka topic/message/offset isolation improvement is claimed.
- No full E2E stack replacement is claimed.
- No change to centrally controlled RepoSync-managed files is approved.
- No local change to RepoSync-managed integration-test resources is proposed for Phase 1.
- Any change to `pre-integration-test/app.py` or generated docker-compose resources would need upstream RepoSync ownership review.
- Kafka + Schema Registry remain the higher-value follow-up candidate.

---

## Implementation Gaps Before Coding

| Gap | Current status | Remaining decision |
|-----|----------------|--------------------|
| Maven module selector | Partially resolved: root POM shows module path `cmd-adaptor-sns-integration-tests` | Validate exact command using this module path |
| Maven profile design | Not confirmed: `local-testcontainers` was not found in current grep output | Add or confirm an opt-in profile |
| Testcontainers dependency | Not confirmed: no Maven dependency found from current grep output | Add or confirm Testcontainers dependencies |
| Test framework alignment | Not confirmed from current grep output | Inspect integration-tests POM for JUnit 4/JUnit 5/Failsafe/Surefire setup |
| Redis client dependency | Still open | Rerun Redis client grep using extended regex and decide dependency |
| Smoke test class design | Not implemented: `MinimalRedisTest` was not found | Create package, class name, method and assertions |
| CI decision | Still open | Keep Phase 1 local/opt-in until reviewed |

Until the remaining items are confirmed, T2.5 should be treated as a pilot planning document rather than an implemented design. The next step is to inspect `cmd-adaptor-sns-integration-tests/pom.xml`, confirm the Maven profile, test framework and dependency approach, then create `MinimalRedisTest.java`.

---

## 14. Decision Still Pending

- Exact integration-tests POM plugin/profile setup.
- Testcontainers dependency approach.
- JUnit/Surefire/Failsafe alignment.
- Redis client dependency choice.
- `MinimalRedisTest.java` implementation details.
- Whether a local opt-in profile should be added.
- Whether any CI smoke profile should ever be used.
- Whether Option B should use config override, Docker network alias or central RepoSync change.

---

## 15. Recommended Next Action

`Inspect cmd-adaptor-sns-integration-tests/pom.xml, confirm the existing test plugin setup, then add a local opt-in Testcontainers profile and MinimalRedisTest.java if they do not already exist.`

Use the `reposync.py` evidence to avoid local edits to RepoSync-managed files and keep Phase 1 limited to a separate opt-in Redis Testcontainers smoke test.
