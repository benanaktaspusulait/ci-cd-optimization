# T2.2 — Capture CI/CD Pipeline Baseline

| Field | Value |
|-------|-------|
| **ID** | T2.2 |
| **Type** | Research |
| **Status** | Draft baseline — SNS estimates retained; DVLA pipeline shape validated from provided source excerpts; Drone UI timing confirmation pending |
| **Date drafted** | 2026-06-11 |
| **Depends on** | T2.1 |

---

## 1. Pipeline Baseline Metrics

The baseline timing numbers below remain estimates for the working pilot recommendation, `fdp-cmd-adaptor-sns`. `fdp-cmd-adaptor-dvla` has been checked from the provided source excerpts as the lightweight comparison candidate to validate that the same pipeline structure is portable across the FDP adaptor pipeline family. Actual timings still require Drone UI data.

### CI Pipeline Duration (estimated from .drone.star analysis)

| Step | Estimated Duration | Notes |
|------|-------------------|-------|
| RepoSync Version | ~2s | Alpine echo |
| Retrieve Artifactory Secrets | ~5-10s | Vault read |
| Wait for Docker | ~5-15s | DIND startup |
| Extract Adaptor Information | ~10-20s | Maven help plugin |
| Kafka & Redis | ~60-120s | Zookeeper + Kafka + Schema Registry + Redis + LocalStack startup |
| Aggregators | ~30-60s | 6 JVM services starting + image pulls from Artifactory |
| mvn clean install | ~3-5 min | Full compile + unit tests (5 modules) |
| Command Adaptor | ~30-60s | Docker build + app startup (detached) |
| Pre-Integration Tests | ~10-30s | Wait script |
| Integration Tests | ~2-4 min | Wait checks + Cucumber test execution |
| Sonar Scan | ~1-2 min | Only on develop |
| Scan with Trivy | ~30-60s | Image vulnerability scan |
| Slack | ~5s | curl notification |
| **Total CI pipeline** | **~10-15 min** | |

### DVLA Lightweight Pipeline Comparison

| Criterion | fdp-cmd-adaptor-sns | fdp-cmd-adaptor-dvla | Notes |
|-----------|:-------------------:|:--------------------:|-------|
| RepoSync pipeline family | Yes | Yes | DVLA `.drone.star` includes the RepoSync warning and `dde-adaptor-reposync` source reference |
| Command adaptor name | `sns` | `dvla` | DVLA sets `COMMAND_ADAPTOR_NAME = 'dvla'` |
| DIND service | Yes | Yes | DVLA uses the same DIND-backed Docker execution pattern |
| Kafka / Redis startup step | Yes | Yes | DVLA has a `Kafka & Redis` pipeline step using docker-compose and wait checks |
| Aggregator startup step | Yes | Yes | DVLA starts 6 aggregators in Drone; compose/Maven also define `aggregate-v1id-v2id` |
| Maven build step | `mvn clean install` | `mvn clean install` | Confirms duplicate-build risk is portable |
| Command adaptor compose build | Yes | Yes | DVLA builds `command-adaptor` from `cmd-adaptor-dvla` |
| Integration-test container | Yes | Yes | DVLA integration-test service runs `mvn -Pci-snapshot install` |
| Sonar scan | Develop only | Develop only | DVLA source excerpt shows develop-branch Sonar condition |
| Trivy scan | Report-only | Report-only | DVLA uses Trivy with `--exit-code 0` and CRITICAL/HIGH severity |
| Artifactory publish docker build | Yes | Yes | DVLA publish step builds `cmd-adaptor-dvla/Dockerfile` from `cmd-adaptor-dvla` |
| Real timing data | Pending Drone UI | Pending Drone UI | DVLA confirms structure, not measured duration |

### Full develop Push Pipeline (CI + ECR/Artifactory + CD + Cleardown)

| Pipeline | Estimated Duration | Depends on |
|----------|-------------------|-----------|
| CI | ~10-15 min | — |
| ARTIFACTORY Adaptor | ~5-8 min | CI (re-runs Maven build + docker push) |
| ARTIFACTORY E2E | ~3-5 min | CI |
| CD (deploy to dev) | ~5-10 min | Artifactory |
| Cleardown | ~3-5 min | CD |
| **Total** | **~25-40 min** | |

---

## 2. Stage Breakdown

### Build Stage

| Metric | Estimated Value |
|--------|-----------------|
| Maven compile + unit test (all modules) | ~3-5 min |
| Docker build (command-adaptor image) | ~30-60s |
| Docker build (repeated in Artifactory pipeline) | ~30-60s |
| **Total build-related work** | **~5-7 min** |

### Integration Test Stage

| Metric | Estimated Value |
|--------|-----------------|
| Infrastructure startup (Kafka, Redis, ZK, SR, LocalStack) | ~60-120s |
| Aggregator startup (6 services) | ~30-60s |
| Command Adaptor build + startup | ~30-60s |
| Wait checks (sequential) | ~30-60s |
| Cucumber test execution | ~1-3 min |
| **Total integration test** | **~4-7 min** |

### Scan Stage

| Metric | Estimated Value |
|--------|-----------------|
| Sonar scan | ~1-2 min |
| Trivy scan | ~30-60s |
| **Total scan** | **~2-3 min** |

---

## 3. Failed Pipeline / Flaky Rate

| Metric | Value | Notes |
|--------|-------|-------|
| Failed pipeline frequency | Unknown — pending Drone UI data | Check last 20 runs on develop |
| Common failure modes | Infrastructure timeout (Kafka/Redis startup), Maven dependency resolution, integration test flakiness | Based on architecture complexity |
| Transient failure handling | No retry mechanism | Pipeline fails → manual re-trigger |
| DVLA failure-rate evidence | Unknown — source structure only | Provided excerpts validate comparable risk areas but not observed failure frequency |

---

## 4. Data Source and Measurement Method

| Field | Value |
|-------|-------|
| **Source** | Drone CI pipeline UI for real timings; static `.drone.star` / compose / POM analysis for structure |
| **Method** | Average of last N=10 successful develop pushes for SNS; DVLA source excerpts used only for lightweight structural comparison unless DVLA Drone timing data is also collected |
| **Metric captured** | Per-step duration + total pipeline duration |
| **Frequency** | Once for baseline (now); once for "after" (post-optimisation) |
| **Tool** | Manual read from Drone UI; no API/automation currently |

### How to Re-measure

1. Open Drone UI for `fdp-cmd-adaptor-sns`
2. Filter by branch: `develop`, status: `success`
3. Select last 10 runs
4. For each run: record total duration + per-step durations
5. Calculate average per step and total
6. Record in metrics template

For the DVLA comparison candidate, the same method can be repeated only if measured comparator timings are needed. For Story 2 portability, the current DVLA evidence is limited to source-structure confirmation.

---

## 5. Observations

### Duplicate Maven Build

The `mvn clean install` runs **twice** per develop push:
1. CI pipeline step 7 (full build + unit tests)
2. ARTIFACTORY pipeline step 5 (full build + `dependency:go-offline`)

This means ~3-5 min of Maven build time is duplicated. The Artifactory pipeline could potentially reuse artifacts from CI if a workspace/cache were shared.

### Sequential Execution Model

The CI pipeline cannot fully parallelise because:
- Integration tests need infra (Kafka, Redis) AND aggregators AND the command adaptor
- These have sequential dependencies: infra → aggregators → adaptor → tests
- No way to pre-cache or pre-pull images between runs (ephemeral DIND)

### No Pipeline Caching

- No Maven dependency cache between runs (ephemeral pods)
- No Docker layer cache between runs (ephemeral DIND)
- Every run downloads all dependencies from scratch

### DVLA Portability Signal

The DVLA source excerpts show the same high-cost pipeline pattern: RepoSync-managed Drone pipeline, DIND, docker-compose orchestration, full Maven build, command-adaptor image build, integration-test container, Trivy scan and Artifactory publish. This supports using SNS as the working pilot while treating DVLA as evidence that the baseline findings are likely portable across the FDP adaptor family.

---

## 6. Baseline Summary (to transfer to metrics template)

| Metric | Baseline (estimated) | Target | Source |
|--------|---------------------|--------|--------|
| CI pipeline duration (avg) | ~10-15 min | — | Drone UI (to confirm) |
| Full pipeline duration (develop push) | ~25-40 min | — | Drone UI (to confirm) |
| Build stage duration | ~3-5 min | — | Drone UI (to confirm) |
| Integration test stage duration | ~4-7 min | — | Drone UI (to confirm) |
| Failed pipeline rate | Unknown | No regression | Drone UI (last 20 runs) |
| Maven build (CI + Artifactory combined) | ~6-10 min | — | Two separate Maven invocations |
| DVLA pipeline-shape portability | Confirmed from provided source excerpts | — | `.drone.star`, POM and compose excerpts |

> ⚠️ **Action needed:** These estimates must be validated against real Drone UI data. Update this document with actual figures once Drone access is confirmed.

---

## Acceptance Criteria

- [x] Baseline pipeline metrics are documented (Section 1-2, estimated)
- [x] Data source / measurement method is recorded (Section 4)
- [x] Metrics are in a form that can be re-measured later for before/after comparison (Section 4, "How to Re-measure")
- [x] DVLA lightweight comparison candidate is checked for pipeline-shape portability from provided source excerpts

> **Note:** Estimates are based on `.drone.star` analysis and typical workload sizes. DVLA validates pipeline shape only. Real timing values from Drone UI should replace the estimated SNS numbers.
