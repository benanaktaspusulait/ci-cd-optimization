# T2.1 — Compare Candidate Pipelines and Select Pilot Repo

| Field | Value |
|-------|-------|
| **ID** | T2.1 |
| **Type** | Analysis |
| **Status** | Analysis complete — SNS selected as working pilot recommendation; DVLA lightweight portability comparison complete; stakeholder confirmation pending |
| **Last updated** | 2026-06-23 |
| **Depends on** | T1.2 |

---

## 1. Candidate Assessment

### Selected Repository: `fdp-cmd-adaptor-sns`

This is the repository we are currently working in. Based on Story 1 findings, this repo is already well-understood and is the current working recommendation for the pilot target.

### Selection Rationale

| Criterion | fdp-cmd-adaptor-sns | Notes |
|-----------|:-------------------:|-------|
| Pipeline complexity | High | Full CI pipeline: Maven build + 6 aggregators + Kafka + Redis + LocalStack + integration tests |
| Docker Compose usage | Heavy | 20 services defined in docker-compose.yml (17 started in CI) |
| Integration test complexity | High | Cucumber-based, multiple wait cycles, full stack required |
| RepoSync-managed | Yes | Standard FDP adaptor — patterns are likely reusable to others |
| Current delivery priority/risk | Low | SNS adaptor is stable — safe to experiment on |
| Portability | High | FDP adaptors appear to use the same RepoSync pipeline family/template |
| Java version | 17 | Testcontainers compatible |
| Build tool | Maven (multi-module) | Standard for all FDP repos |

### Candidate Comparison

`fdp-cmd-adaptor-dvla` is used as the lightweight second candidate. The purpose is not to run a full Story 1 assessment on DVLA, but to confirm whether the SNS findings are portable across the FDP adaptor pipeline family.

The DVLA source excerpts in `dvla.txt` include the parent/application/integration-test POMs, RepoSync-controlled Dockerfile, RepoSync `.drone.star`, and RepoSync-controlled Docker Compose configuration. That is enough for a lightweight portability check, but not for stakeholder delivery-risk sign-off.

The DVLA lightweight comparison supports the conclusion that SNS and DVLA appear to use the same FDP command-adaptor RepoSync pipeline family and the same core CI/DIND/Docker Compose orchestration model. The differences are adaptor-specific metadata, schema/mapping dependencies, topic suffixes and business test data, not the underlying CI/CD architecture.

| Criterion | fdp-cmd-adaptor-sns | fdp-cmd-adaptor-dvla | Notes |
|-----------|:-------------------:|:--------------------:|-------|
| Same RepoSync template family | Yes | Yes | DVLA excerpts include RepoSync warnings and RepoSync `.drone.star` with version 7.1.0 |
| Pipeline complexity | High | High | DVLA `.drone.star` includes DIND, Maven build, compose startup, integration tests, Trivy and publish flows |
| Docker Compose usage | Heavy | Heavy | DVLA uses RepoSync-controlled Docker Compose plus Maven `docker-compose-maven-plugin` |
| Integration-test complexity | High | High | DVLA has `cmd-adaptor-dvla-integration-tests`, `local-int-*` profiles, `ci-snapshot`, aggregators and compose dependencies |
| Dockerfile ownership | RepoSync-controlled | RepoSync-controlled | DVLA Dockerfile contains central RepoSync warning |
| Testcontainers suitability | High | Likely similar | DVLA is Java 17/Maven with similar DIND/Compose constraints; Testcontainers still needs a dedicated pilot task |
| Build optimisation potential | High | High | DVLA uses a RepoSync-controlled `amazoncorretto:17` Dockerfile and template-driven build/publish flow |
| Delivery risk | Low | TBC | Needs stakeholder input; DVLA is comparison-only unless the pilot scope changes |
| Access/source available | Yes | Source excerpts available; CI access TBC | Pilot needs practical source and CI access |
| Suitability for pilot | High | Comparison only | SNS remains selected because it is already analysed, understood and lower-risk |

### Why This Repo (and not another)

1. **Representative** — appears to use the same RepoSync pipeline family/template as other FDP adaptors
2. **Accessible** — we have the source code and can run builds locally
3. **Low risk** — SNS feed is stable; changes won't disrupt active delivery
4. **Complex enough** — has the full integration test stack (Kafka, Redis, Schema Registry, LocalStack, 6+ aggregators)
5. **Portable patterns** — improvements here can be evaluated for transfer to other adaptors via RepoSync

### Portability Notes

| What transfers to other adaptors | What is SNS-specific |
|----------------------------------|---------------------|
| Dockerfile optimisations (adaptors appear to use the same template family) | AVRO schema specifics (`sns-ens`) |
| `.dockerignore` (same build structure) | Feed-specific test data |
| Testcontainers setup (same infra deps) | `LOCALSTACK_SERVICES=iam` |
| Maven profile approach | `KAFKA_TOPIC_SUFFIX=sns` |
| Compose service mapping (same services) | Adaptor-specific business logic |
| Pipeline observations (same `.drone.star`) | — |

---

## 2. Project Structure

```
fdp-cmd-adaptor-sns/
├── pom.xml                          (parent POM, multi-module)
├── cmd-adaptor-sns-avro/            (AVRO schema generation)
├── cmd-adaptor-sns-common/          (shared code)
├── cmd-adaptor-sns-test-common/     (shared test utilities)
├── cmd-adaptor-sns/                 (main application + Dockerfile)
│   ├── pom.xml
│   ├── Dockerfile                   ← RepoSync controlled
│   └── src/main/java/...
└── cmd-adaptor-sns-integration-tests/  (integration tests)
    ├── pom.xml                      ← profiles: local, ci-snapshot, ci-cmd, etc.
    └── src/test/resources/
        └── docker-compose/
            └── docker-compose.yml   ← RepoSync controlled
```

### Key Properties

| Property | Value |
|----------|-------|
| Group ID | `uk.gov.ho.dacc.fdp` |
| Artifact ID | `fdp-cmd-adaptor-sns` |
| Version | `2.3.2-SNAPSHOT` |
| Java version | 17 |
| Spring Boot | TBC — verify against the pilot repo POM before final Jira/sign-off |
| Aggregator Core | 10.3.1 |
| Test framework | Cucumber + JUnit 5 (vintage engine) |
| Build tool | Maven (multi-module, 5 modules) |

---

## 3. Maven Profiles (Integration Tests)

| Profile | Use case | Skips containers? | Skips tests? |
|---------|----------|:-----------------:|:------------:|
| `local` (default) | Normal dev — no tests | Yes | Yes |
| `local-int-cmd` | Local integration (cmd only, no aggregators) | No | No |
| `local-int-snapshot` | Local integration (full stack) | No | No |
| `ci-cmd` | 6point6 GitHub Workflow | Yes (external) | No |
| `ci-snapshot` | **HO ACP GitLab/Drone** — used in CI pipeline | Yes (external) | No |
| `k8s-dacc-dde-dev` | K8s dev namespace | Yes (external) | No |
| `k8s-dacc-dde-test` | K8s test namespace | Yes (external) | No |
| `k8s-dacc-fdp-dev` | K8s FDP dev namespace | Yes (external) | No |

The `ci-snapshot` profile is what the CI pipeline's `integration-tests` docker-compose container uses. It sets `skip.containers=true` because the compose orchestration handles infra externally.

---

## 4. Selection Output

| Field | Value |
|-------|-------|
| Selected repository | `fdp-cmd-adaptor-sns` |
| Compared candidate(s) | `fdp-cmd-adaptor-sns` selected as pilot recommendation; `fdp-cmd-adaptor-dvla` used as lightweight portability comparison |
| GitLab project URL | To be confirmed; current working assumption is `https://gitlab.digital.homeoffice.gov.uk/dacc-aws/fdp-cmd-adaptor-sns` |
| GitLab environment | Self-hosted (`gitlab.digital.homeoffice.gov.uk`) |
| Issue / board tracker | Jira (CST-2000 series) |
| Primary language / build tool | Java 17 / Maven |
| Selection rationale | Representative, low-risk, full complexity, portable patterns |
| Portability notes | DVLA source excerpts support portability across the FDP adaptor pipeline family for pipeline shape, RepoSync ownership, Dockerfile, Compose and Maven profile patterns; stakeholder/CI access confirmation is still needed before wider rollout |
| Stakeholder who agreed scope | _To be confirmed with Thomas Reddy_ |

---

## Acceptance Criteria

- [x] At least two candidate repositories/pipelines are compared — DVLA lightweight comparison completed from provided source excerpts
- [x] One candidate repository/service is selected — `fdp-cmd-adaptor-sns`
- [x] Selection rationale is documented (Section 1)
- [x] Portability notes are captured (Section 1, table)
- [ ] Pilot scope is agreed with relevant stakeholders — pending confirmation with Thomas Reddy
