# E2-S1.1 — SNS Image-Build Implementation Result

| Field | Value |
|---|---|
| **Status** | **Local implementation validated — durable RepoSync ownership/adoption route pending** |
| **Date** | 2026-08-03 |
| **Target repository** | `/Users/benanaktas/project/home-office/test1` |
| **Starting commit** | `409fd61160925878cddf12fbde9f64fd37ce1a88` |
| **Working branch** | `codex/feature-sns-docker-layer-ordering` |
| **Commit created** | None |

## Repository State

The target repository was clean on `main` before the branch was created. The
only current tracked-file change is `cmd-adaptor-sns/Dockerfile`.
`cmd-adaptor-sns/.dockerignore`, the pilot prototype and the pilot warm-cache
script were already tracked at the starting commit. No Maven, Compose, Drone,
Testcontainers, application or test file was changed.

## Ownership Route

The production Dockerfile explicitly states that it is centrally controlled,
that target-repository edits will be overwritten and that its durable source is:

```text
https://gitlab.digital.homeoffice.gov.uk/dacc-de/dde-adaptor-reposync
```

The inspected repository does not provide equivalent RepoSync-managed wording
for `cmd-adaptor-sns/.dockerignore`. It also does not identify the responsible
person or approval group for either file. Therefore:

- this branch is a temporary, reviewable target-repository implementation;
- no commit, merge or durable adoption was performed;
- the Dockerfile change must be proposed through RepoSync and its platform owner;
- the durable home and reviewer for `.dockerignore` still require confirmation.

The ownership acceptance criterion remains open until the owner details and
accepted review route are recorded.

## Implemented Changes

The production Dockerfile now uses the exact layer shape validated by the T3.3
prototype:

1. retain the existing `amazoncorretto:17` base;
2. perform the existing `yum`, `envconsul` and user setup before copying runtime
   artefacts;
3. copy the application JAR and OpenTelemetry agent afterwards;
4. apply the existing ownership and permissions in a small post-copy layer;
5. preserve the existing user, work directory and `CMD`.

After removing the production file's RepoSync warning header, its content is
byte-for-byte identical to `Dockerfile.layer-order-prototype`. `git diff
--check` passes.

The targeted `.dockerignore` was reviewed and retained unchanged. It excludes
source and unrelated generated output while explicitly re-including the only
two inputs copied by this Dockerfile:

```text
target/cmd-adaptor-sns-exec.jar
target/dependencies/opentelemetry-javaagent.jar
```

The Dockerfile does not copy startup scripts, certificates or configuration
files, so none are hidden by this build-context rule.

## Reviewable Diff

```diff
-COPY ./target/cmd-adaptor-sns-exec.jar /local
-COPY ./target/dependencies/opentelemetry-javaagent.jar /local/opentelemetry-javaagent.jar
-
 WORKDIR /tmp

 RUN yum install -y ...
@@
-      fdpuser \
-    && chmod 0755 /local/cmd-adaptor-sns-exec.jar \
+      fdpuser
+
+COPY ./target/cmd-adaptor-sns-exec.jar /local/cmd-adaptor-sns-exec.jar
+COPY ./target/dependencies/opentelemetry-javaagent.jar /local/opentelemetry-javaagent.jar
+
+RUN chmod 0755 /local/cmd-adaptor-sns-exec.jar \
     && chown fdpuser:fdpuser /local/cmd-adaptor-sns-exec.jar \
```

## Explicit Non-Changes

The implementation does not change the base image, package list, `envconsul`
version, application artefact content, OpenTelemetry version, entrypoint,
runtime command, debug/JMX flags, user, final work directory, Maven lifecycle,
Compose, Drone or Testcontainers.

## Environment

```text
Host: macOS 26.5.2 (25F84), arm64
Host Java: Amazon Corretto 21.0.7
Host Maven: 3.9.9
Docker Desktop: 4.63.0 (220185)
Docker client/server engine: 29.2.1, API 1.53
Docker context: desktop-linux
Docker server: linux/arm64, overlay2, 10 CPUs, 12,528,578,560 bytes memory
Starting image state: no cmd-adaptor-sns images were present
```

The repository's CI Maven image
`quay.io/ukhomeofficedigital/ileap-java17-mvn:1.3` was inspected as a possible
recovery path. It is `linux/amd64`, uses Maven 3.9.6 and expects Artifactory
credentials through environment variables. Those credentials were not present
in this local execution environment, so the image did not remove the blocker.

## Build Prerequisite Failure

The required Docker inputs were absent:

```text
cmd-adaptor-sns/target/cmd-adaptor-sns-exec.jar
cmd-adaptor-sns/target/dependencies/opentelemetry-javaagent.jar
```

The exact prerequisite command attempted was:

```bash
mvn -pl cmd-adaptor-sns -am -DskipTests package
```

It failed before compilation because `uk.gov.ho.dacc.fdp:fdp-bom:pom:3.3.2`
could not be resolved from the repositories available to the host Maven
configuration. The missing Spring Kafka, Kafka client and JUnit versions were
consequences of that unresolved BOM. No invented or substituted dependency was
used.

## Cold Build

The requested command was attempted exactly:

```bash
/usr/bin/time -p docker build --no-cache -f cmd-adaptor-sns/Dockerfile -t cmd-adaptor-sns:e2-s1-1-cold cmd-adaptor-sns
```

Observed result:

```text
Result: failed before an image was produced
Elapsed: real 2.11s
Dockerfile transfer: 2.31kB
.dockerignore transfer: 764B
Build context transfer: 2B
Base image digest: sha256:566a76d7a379e3f1e6ceb450bddd66bb5d1f94a7605d81d2ee3404230377769d
Failure: both required target JAR paths were not found
Final image state: cmd-adaptor-sns:e2-s1-1-cold does not exist
```

The `2B` context is a missing-artefact failure state and is not comparable to
the pilot's valid `189B` context measurement. Although BuildKit displayed the
setup steps as cached while solving the graph, the build failed at the `COPY`
inputs and this is not a successful cold build.

After the required artefacts became available, the user reran the documented
no-cache command successfully on the primary machine. The raw duration, image
ID and image size were not transferred into this evidence record, so the
successful run is recorded qualitatively and no new cold-build improvement is
claimed. The failed `2.11s` precondition check above remains recorded only as
the initial environment failure, not as cold-build performance evidence.

## Warm No-Change Build

Subsequently run successfully by the user on the primary machine after the
required Maven artefacts were available. The unchanged rebuild reused the
expected Docker cache. The raw duration was not transferred into this evidence
record, so no new numeric warm-build claim is made.

## Real JAR-Content-Change Warm Build

Subsequently run successfully by the user on the primary machine with a real
JAR content-hash change. The user confirmed that:

- the before/change/restore hash behaviour matched the documented method;
- the expensive `yum`/`envconsul` setup layer was reused from cache;
- the application-JAR copy and required following layer were rebuilt;
- the original valid JAR was restored afterwards.

The raw hashes and duration were not transferred into this evidence record.
They are therefore not reproduced or estimated here. Quantitative timing
continues to come only from the preserved T3.3/T3.4 pilot evidence.

## Runtime and Artefact Validation

Subsequently completed successfully by the user on the primary machine. The
reported smoke result confirmed Java and `envconsul`, both required runtime
JARs, `fdpuser`, `/home/fdpuser` and the preserved Java/JMX/OpenTelemetry
command. This is a qualitative successful production-file validation; raw
command output was not transferred and no unrecorded numeric result is claimed.

## Failures and Retries

1. Host Maven package failed because the private FDP BOM was not resolvable.
2. The CI Maven image was inspected; it requires unavailable Artifactory
   environment credentials.
3. One cold Docker build failed because the two Maven-produced JARs were absent.
4. No synthetic JAR, fabricated timing or successful runtime claim bypassed the
   blocker.
5. The user later reran the documented Maven, cold, warm, real-JAR-change and
   runtime checks on the primary machine with the required artefacts available
   and reported that all checks produced the expected successful results.

## Comparison with Pilot Evidence

The diff implements the T3.3 candidate previously measured under a
same-Docker-daemon, warm-cache, real-JAR-content-change method. The prior
`75.82–77.90s` and `4.62–5.08s` measurements remain the preserved quantitative
pilot evidence. The production-file run qualitatively reconfirmed the expected
cache behaviour, but its raw timing was not retained here and is not presented
as a new numeric comparison.

## Non-Claims

This result does not claim:

- a new numeric cold-build, image-size, CI, production, reliability or cost
  improvement;
- that local BuildKit cache behaviour will apply to Drone/DIND;
- runtime equivalence based only on a textual Dockerfile comparison;
- durable RepoSync adoption or owner approval.

## Rollback

Because no commit was created, the executable local rollback is:

```bash
git -C /Users/benanaktas/project/home-office/test1 restore -- cmd-adaptor-sns/Dockerfile cmd-adaptor-sns/.dockerignore
```

After rollback, regenerate artefacts in an authorised environment and run:

```bash
mvn -pl cmd-adaptor-sns -am -DskipTests package
docker build --no-cache -f cmd-adaptor-sns/Dockerfile -t cmd-adaptor-sns:e2-s1-1-rollback cmd-adaptor-sns
docker run --rm --entrypoint sh cmd-adaptor-sns:e2-s1-1-rollback -c 'java -version && envconsul --version && test -f /local/cmd-adaptor-sns-exec.jar && test -f /local/opentelemetry-javaagent.jar'
```

If a later RepoSync commit is approved, rollback must revert that source commit
in RepoSync through the same platform owner and use normal RepoSync propagation
to restore the generated target file.

## Recommendation for E2-S1.3

The implementation is locally validated and may be handed to E2-S1.3 as a CI
evaluation candidate. Before any durable merge or RepoSync propagation, confirm
the RepoSync/platform reviewer and the durable home of `.dockerignore`.

E2-S1.3 must measure real CI independently and must not inherit either the
pilot's local timing or the qualitative production-file validation as CI
evidence.
